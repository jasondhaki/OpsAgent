import type { NextConfig } from "next";

// Host-agnostic security headers (CLAUDE.md §2: no host-specific APIs).
// ponytail: no Content-Security-Policy yet — Next's inline scripts need nonces; add a nonce-based CSP via proxy.ts if a page ever renders untrusted HTML.
const securityHeaders = [
  { key: "Strict-Transport-Security", value: "max-age=63072000; includeSubDomains" },
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "X-Frame-Options", value: "DENY" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=(), payment=()" },
];

const nextConfig: NextConfig = {
  poweredByHeader: false,
  async headers() {
    return [{ source: "/:path*", headers: securityHeaders }];
  },
};

export default nextConfig;
